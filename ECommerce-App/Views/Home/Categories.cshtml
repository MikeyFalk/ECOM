@model IEnumerable<ECommerce_App.Models.Category>

<ul>
    @foreach (var category in Model)
    {
        <li>
            <a asp-controller="Home" asp-action="Category" asp-route-name=@category.type asp-route-type=@category.name>@category.name</a> 
        </li>
    }

</ul>
