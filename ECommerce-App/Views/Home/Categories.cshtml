@model IEnumerable<ECommerce_App.Models.Category>

<ul>
    @foreach (var category in Model)
    {
        <li>
            <a asp-controller="Home" asp-action="Category" asp-route-name=@category.Name asp-route-type=@category.Type>@category.Name</a> 
        </li>
    }

</ul>
